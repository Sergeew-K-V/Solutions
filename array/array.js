// const arr = ["bla bla", "ble ble", "blu blu", "fsd", "fsd", "bla bla"];

// console.log(Array.from(new Set(arr)));

///////////////////////////////////////////////////////////
// function aclean(arr) {
//   let map = new Map();

//   for (let word of arr) {
//     let sorted = word.toLowerCase().split("").sort().join(""); // (*)
//     map.set(sorted, word);
//   }

//   return Array.from(map.values());
// }

// let arr = ["nap", "teachers", "cheaters", "PAN", "ear", "era", "hectares"];

// console.log(aclean(arr));

///////////////////////////////////////////////////////////
// let map = new Map();

// let object = { name: "John" };

// map.set(object, "data");

// object = null;

// console.log(object);
// console.log(map.get(object));
